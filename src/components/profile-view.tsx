"use client";
import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { ArrowUpRight, FlaskConical, LogOut } from "lucide-react";
import type { Profile } from "@/lib/types";
import { profiles, uid } from "@/lib/seed-data";
import { initialDemo } from "@/lib/demo";
import { authenticate, signOut } from "@/app/auth/actions";
import { useDemo, updateDemo } from "./demo-provider";
import { Avatar } from "./avatar";
export function ProfileView({user:liveUser}:{user:Profile|null}) {
  const {state,isDemo,ready}=useDemo();const user=isDemo?state.user:liveUser;
  const [signup,setSignup]=useState(false);const [notice,setNotice]=useState("");const [pending,startTransition]=useTransition();const router=useRouter();
  function demoLogin(creator:boolean) {const result=updateDemo(s=>({...s,user:creator?profiles[0]:{id:uid(900),display_name:"Alex Morgan"}}));setNotice(result.ok?"Demo account ready. Everything you do stays in this browser.":result.message);}
  return <div className="page account-page"><div className="page-heading"><span className="eyebrow">YOUR LITTLE CORNER</span><h1>{user?`Hello, ${user.display_name.split(" ")[0]}.`:"Come on in."}</h1><p>{user?"Your circles, your curiosity, your next good read.":"Find a conversation you love. Make yourself part of it."}</p></div><div className="account-layout"><section className="surface auth-card">{user ? <><div className="profile-identity"><Avatar person={user}/><div><h2>{user.display_name}</h2><span className="muted">{isDemo?"Demo account · Browser-local activity":"Signed in with Supabase"}</span></div></div><Link href="/my-groups" className="button primary">Go to My Groups <ArrowUpRight size={16}/></Link><button className="button" disabled={pending} onClick={()=>startTransition(async()=>{const result=isDemo?updateDemo(s=>({...s,user:null})):await signOut();setNotice(result.message);if(result.ok) router.refresh();})}><LogOut size={16}/>Sign out</button></> : isDemo ? <><span className="demo-label">Demo mode</span><h2>Take a look around.</h2><p>No account setup needed. Try joining and reacting as a reader, or answer questions as a creator.</p><button className="button primary" disabled={!ready} onClick={()=>demoLogin(false)}>Explore as a reader</button><button className="button" disabled={!ready} onClick={()=>demoLogin(true)}>Try the creator dashboard</button><p className="fine-print">Fictional accounts. No email or password collected.</p></> : <><div className="auth-tabs"><button onClick={()=>setSignup(false)} aria-pressed={!signup} className={!signup?"selected":""}>Sign in</button><button onClick={()=>setSignup(true)} aria-pressed={signup} className={signup?"selected":""}>Create account</button></div><form onSubmit={event=>{event.preventDefault();const fields=new FormData(event.currentTarget);startTransition(async()=>{const result=await authenticate({email:fields.get("email"),password:fields.get("password"),display_name:signup?fields.get("display_name"):"Reader"},signup);setNotice(result.message);if(result.ok)router.refresh();});}}>{signup && <label>Display name<input name="display_name" autoComplete="nickname" required minLength={2} maxLength={60}/></label>}<label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Password<input name="password" type="password" autoComplete={signup?"new-password":"current-password"} required minLength={8} maxLength={128}/></label><button disabled={pending} className="button primary">{pending?"One moment…":signup?"Create account":"Sign in"}</button></form></>}{notice && <p className="notice" role="status">{notice}</p>}</section><aside className="account-note"><FlaskConical size={24}/><h2>Built around the conversation.</h2><p>You can always browse without an account. Sign in when you’re ready to react, join a circle, or send a question.</p>{isDemo && <><p>This is a working product demo with fictional content. Demo activity is stored on this device and never represents real traffic.</p>{user && <div className="inline-actions"><button className="text-button" onClick={()=>demoLogin(false)}>Switch to reader</button><button className="text-button" onClick={()=>demoLogin(true)}>Switch to creator</button></div>}<button className="text-button muted" onClick={()=>setNotice(updateDemo(()=>initialDemo).message)}>Reset demo activity</button></>}</aside></div></div>;
}
